- name: install packages
  hosts: localhosts 
  become: yes
  tasks:
  - name: install packages
    ansible.builtin.dnf:
      msg: "{{ item }}"
      state: latest
    loop:
    - mysql
    - nginx   
    - postfix